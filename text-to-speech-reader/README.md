# Text to Speech Reader

## 📌 Project Overview
An accessibility tool that converts text from documents (or user input) into audible speech using offline text-to-speech engines.

## 🎯 Objectives
- To help visually impaired users read documents.
- To provide an interface for listening to text content.

## 🧠 Concepts & Tech Stack
- **NLP / Audio:** pyttsx3 (Text-to-Speech)
- **Language:** Python

## 🚀 How to Run
1.  Install dependencies:
    ```bash
    pip install -r requirements.txt
    ```
2.  Run the reader:
    ```bash
    python src/tts.py
    ```

## 🔮 Future Improvements
- Support for multiple languages.
- Integration with OCR for reading from images.
